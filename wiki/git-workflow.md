
# Git Workflow

## Resolving Merge Conflicts
To resolve a merge conflict, edit the conflicting file, choose which changes to keep, then stage and commit.

## Branching
Create branches with `git branch branch-name`.
